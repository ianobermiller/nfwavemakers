{
  "name": "nfwavemakers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "npm ci --prefix ballots && npm run build --prefix ballots && node _build.mjs"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
